# vim

Install the Vim/Neovim configuration with:

- `make -C vim install`

The `Makefile` targets symlink a single Neovim configuration file into the appropriate Neovim config location.

Important files:
- `init.lua`
- `Makefile`
