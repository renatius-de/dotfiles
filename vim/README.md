# vim

Install the Vim/Neovim configuration with:

- `make -C vim install`

The `Makefile` targets symlink `vimrc` and `nvimrc` into the home directory or the appropriate Neovim config location.

Important files:
- `vimrc`
- `nvimrc`
- `Makefile`
