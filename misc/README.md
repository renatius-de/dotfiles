# misc

Utility scripts for environment setup and Java configuration.

Install the components with:

- `make -C misc install`

Important targets:
- `clean` — removes installed Java runtimes
- `install` — installs Java runtimes and imports CA certificates
- `upgrade` — performs the same steps as `install`

Important files:
- `Makefile`
