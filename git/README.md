# git

Install the Git configuration with:

- `make -C git install`

The `Makefile` targets symlink Git configuration files into the home directory.

Important files:
- `config`
- `delta.gitconfig`
- `ignore`
- `Makefile`

Use `~/.gitconfig.local` for user-specific overrides.
