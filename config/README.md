# config

Additional configuration modules and symlink utilities.

Install the modules with:

- `make -C config install`

This directory currently contains:
- `spring-boot/` — symlinked to `~/.config/spring-boot`

Important files:
- `Makefile`
